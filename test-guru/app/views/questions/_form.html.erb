<h1> <%= question_status(@question)%> </h1>

<%= form_with model: [@test, @question], local: true do |form| %>
  <%= render 'shared/errors', resource: @question %>

  <div>
    <%= form.label :body %>
    <%= form.text_field :body, id: :question_body %>
  </div>

  <%= form.submit %>

  
<% end %>